# Icon

Drop a Windows `.ico` file here named `icon.ico`. When present, the Inno Setup
installer uses it as the setup icon (`SetupIconFile`) and the app's tray/shortcut
icon. If absent, the build falls back to AutoHotkey's default icon.

Recommended: a 256×256 ICO with multiple embedded sizes (16, 32, 48, 256).
